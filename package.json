{
  "name": "clipit",
  "private": true,
  "version": "1.2.3",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy:local": "vite build && mkdir -p ~/html/nsd/clipit && cp -r dist/. ~/html/nsd/clipit/"
  },
  "devDependencies": {
    "vite": "8.0.5"
  }
}
